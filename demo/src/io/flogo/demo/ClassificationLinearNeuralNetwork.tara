dsl Blatt

Training
	Adam
	MAELoss
	GrowthEarlyStopper(patience = 5, threshold = 95)

NeuralNetwork ClassificationLinearNeuralNetwork
	LinearSection
		Input(x=10000)
		Block
			Linear > Output(x=5000)
			ReLU
		Block
			Linear > Output(x=2000)
			ELU > alpha = 1
		Block
			Linear > Output(x=800)
			ReLU
		Block
			Linear > Output(x=200)
			ReLU
		Block
			Linear > Output(x=10)
			ReLU

	SoftmaxSection
