dsl Flogo

Laboratory
	Adam
	MAELoss
	RegressionStrategy
	Dataset(name = "cats and dogs", batchSize = 10)
		Split(train=0.7, test=0.2, validation=0.1)

	Experiment e626
		Adagrad
		Substitute(id = "01") > BatchNormalization(eps = 0.1)

Architecture RecurrentNeuralNetwork
	RecurrentSection
		Input(x=1000)
		Block as Residual
			CustomDownSampling

			VLayer > id = "01"
			LSTM > Output(x=10)
			BatchNormalization
			Dropout


