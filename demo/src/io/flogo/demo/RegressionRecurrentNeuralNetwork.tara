dsl Flogo

Laboratory
	Adam
	MAELoss
	RegressionStrategy
	Dataset(name = "cats and dogs", batchSize = 10)
		Split(train=0.7, test=0.2, validation=0.1)

	Experiment e626
		Adagrad
		Substitute(id = "01")
			Linear > Output(x=10)


Architecture RecurrentNeuralNetwork
	RecurrentSection
		Input(x=300, seqLength = 4)
		Block
			LSTM(bidirectional  = true, numLayers = 4)
            	LastCellState > Output(x=20)
			VLayer(id = "01")
			BatchNormalization
			Dropout
