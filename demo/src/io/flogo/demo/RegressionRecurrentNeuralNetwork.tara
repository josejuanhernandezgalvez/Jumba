dsl Flogo

Evolution(name = "")
	Adam
	MAELoss
	Dataset(name = "cats and dogs", train=0.8, test=0.2, batchSize = 10)
		path = ""
	Cycle(epochs = 10)
		fit = 0.7
		val = 0.3

Architecture(name = "") RecurrentNeuralNetwork
	RecurrentSection
		Input(x=1000)
		Output(x=10)
		Type("LSTM")
    	OutputElement("lastOutput")

Lifecycle
	Server("example server")
