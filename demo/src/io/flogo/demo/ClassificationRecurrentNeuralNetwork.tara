dsl Flogo

Laboratory
	Adam
	MAELoss
	ClassificationStrategy
	Dataset(name = "cats and dogs", batchSize = 10)
		Split(train=0.7, test=0.2, validation=0.1)

	Experiment e626
		Adagrad

Architecture
	RecurrentSection
		Input(x = 300, seqLength = 4)
		Block
			LSTM
				EndSequence > Output(x=100)

	FlattenSection

	LinearSection
		Block
			Linear > Output(x=5000)
			ReLU
		Block
			Linear > Output(x=10)
			ReLU
			Softmax